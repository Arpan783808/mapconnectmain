import { FunctionComponent } from "react";
import styles from "./AfterLogin.module.css";

const AfterLogin: FunctionComponent = () => {
  return <div className={styles.afterLogin} />;
};

export default AfterLogin;
